"""Deterministic safety engines — the provable core of Codoctor.

These modules make the high-stakes decisions (danger signs, drug safety) with
plain, auditable rules. The LLM never makes these calls; it only narrates them.
"""
